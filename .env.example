################################################################################
# 🌐 PAYROLL SAAS — ENVIRONMENT CONFIGURATION EXAMPLE
# Ce fichier explique à tout développeur comment configurer l’environnement
# et où récupérer les clés/API nécessaires pour faire tourner la plateforme.
#
# ⚠️ NE PAS METTRE DE CLÉS SENSIBLES ICI.
# ⚠️ TOUJOURS CRÉER UNE COPIE : ".env" (non committée dans Git)
################################################################################


################################################################################
# 🗄️ DATABASE — NEON POSTGRESQL
# Service : https://neon.tech
# Crée ta base → Database → Connection Details → "postgresql connection string"
################################################################################
DATABASE_URL="postgresql://USER:PASSWORD@HOST/dbname?sslmode=require"


################################################################################
# 🔐 NEXTAUTH — AUTHENTIFICATION
# Documentation : https://next-auth.js.org/
################################################################################
NEXTAUTH_SECRET="GENERATE_WITH: openssl rand -base64 32"
NEXTAUTH_URL="http://localhost:3000"


################################################################################
# ☁️ AWS S3 — STOCKAGE DOCUMENTS & CONTRATS
# Service : https://aws.amazon.com/s3/
################################################################################
AWS_PROFILE=hosted_storage
AWS_REGION=us-west-2
AWS_BUCKET_NAME=your-bucket-name
AWS_FOLDER_PREFIX=folder/


################################################################################
# 🚀 REDIS — UPSTASH (BullMQ, Rate-limit, Webhooks)
# Service : https://upstash.com
# Upstash Console → Your Database → Redis Connect
################################################################################

# ⚠️ REQUIRED FOR BULLMQ: TCP Endpoint (ioredis)
# Console → Your Database → Redis Connect → ioredis
# Format: rediss://default:<password>@<host>:<port>
UPSTASH_REDIS_URL="rediss://default:YOUR_PASSWORD@your-redis.upstash.io:6380"

# REST API Endpoint (for direct HTTP calls, not used by BullMQ)
# Console → Your Database → REST API
UPSTASH_REDIS_REST_URL="https://YOUR-UPSTASH-URL.upstash.io"
UPSTASH_REDIS_REST_TOKEN="YOUR_UPSTASH_REST_TOKEN"


################################################################################
# 📧 EMAIL SERVICE — RESEND (Recommandé, Next.js natif)
# Service : https://resend.com
# Dashboard → API Keys
################################################################################
EMAIL_PROVIDER=resend
RESEND_API_KEY="YOUR_RESEND_API_KEY"
EMAIL_FROM="noreply@your-domain.com"


################################################################################
# 📱 SMS SERVICE — TWILIO (SMS & Notifications)
# Service : https://www.twilio.com/console
################################################################################
SMS_PROVIDER=twilio

# Twilio Account SID & Auth Token
# Dashboard → Account → Keys & Credentials
TWILIO_ACCOUNT_SID="YOUR_TWILIO_SID"
TWILIO_AUTH_TOKEN="YOUR_TWILIO_TOKEN"

# Numéro Twilio acheté
# Console → Phone Numbers → Manage → Active Numbers
TWILIO_PHONE_NUMBER="+1234567890"
SMS_FROM="+1234567890"


################################################################################
# 🪵 LOGGING — Niveau des logs
# Options : error, warn, info, http, debug
################################################################################
LOG_LEVEL=info
